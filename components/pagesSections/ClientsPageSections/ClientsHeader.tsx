import { Typography } from "@/components";

export function ClientsHeader() {
  return (
    <header>
      <Typography component="h1" variant="h3">
        Mes clients (42)
      </Typography>
    </header>
  );
}
